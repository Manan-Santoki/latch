# Google OAuth "Chrome Extension" client ID.
# This is PUBLIC build configuration, not a secret — there is no client secret
# that can be safely stored in a browser extension.
# Created during OAuth setup; see docs/GOOGLE-OAUTH-SETUP.md.
#
# Copy this file to `.env` and fill in the value:
WXT_GOOGLE_CLIENT_ID=
